========================================================================
       WIN32 APPLICATION : minmaxWin32
========================================================================
Проект  "minmaxWin32" - чистый Win32 API
Среда  "Visual С++ (Microsoft)" 
 написан в 2000 году.
Функциональное ограничение: нет
Запуск:
  1. из командной строки:
   minmaxWin32.exe   file1  file2  {1||2}
здесь 
   file1  -  файл с исходной таблицей (таблица = квадратная матрица (<>0))
   file2  -  файл рассчитанных  данных (таблица)
   1      -  расчет минимального расстояния
   2      -  расчет максимального расстояния
  
   2.из Windows :

Программа рассчитывает 'Оптимальное суммарное значение', 'Путь по вершинам'  и создает таблицу расстояний.

 для использования по предмету  "Мат. моделирование." 


ЧЕ.
/////////////////////////////////////////////////////////////////////////////
AppWizard has created this minmaxWin32 application for you.  

This file contains a summary of what you will find in each of the files that
make up your minmaxWin32 application.

minmaxWin32.cpp
    This is the main application source file.

minmaxWin32.dsp
    This file (the project file) contains information at the project level and
    is used to build a single project or subproject. Other users can share the
    project (.dsp) file, but they should export the makefiles locally.
/////////////////////////////////////////////////////////////////////////////
AppWizard has created the following resources:

minmaxWin32.rc
    This is a listing of all of the Microsoft Windows resources that the
    program uses.  It includes the icons, bitmaps, and cursors that are stored
    in the RES subdirectory.  This file can be directly edited in Microsoft
	Visual C++.

res\minmaxWin32.ico
    This is an icon file, which is used as the application's icon (32x32).
    This icon is included by the main resource file minmaxWin32.rc.

small.ico
    %%This is an icon file, which contains a smaller version (16x16)
	of the application's icon. This icon is included by the main resource
	file minmaxWin32.rc.
/////////////////////////////////////////////////////////////////////////////
Other standard files:

StdAfx.h, StdAfx.cpp
    These files are used to build a precompiled header (PCH) file
    named minmaxWin32.pch and a precompiled types file named StdAfx.obj.

Resource.h
    This is the standard header file, which defines new resource IDs.
    Microsoft Visual C++ reads and updates this file.

/////////////////////////////////////////////////////////////////////////////
Other notes:

AppWizard uses "TODO:" to indicate parts of the source code you
should add to or customize.


/////////////////////////////////////////////////////////////////////////////
